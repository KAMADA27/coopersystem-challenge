import { AmountToRedeemValidatorDirective } from './amount-to-redeem-validator.directive';

describe('AmountToRedeemValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountToRedeemValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
